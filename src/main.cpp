#include "src/ui files/Ui/ui.h"
#include "src/classes/Mat2D/Mat2D_LN.h"

int main(int argc, char *argv[]) {
//    Mat2D_LN m(3, 3);
//    for (int i = 0; i < m.size(); ++i) {
//        for (int j = 0; j < m[0].size(); ++j) {
//            std::cin >> m[i][j];
//        }
//    }
//    auto q = m.lu_decomposition();
//    std::get<0>(q).print();
//    std::get<1>(q).print();
//    std::get<2>(q).print();
//    std::get<3>(q).print();
//    std::cout << m.det();
    return start_UI(argc, argv);
}
